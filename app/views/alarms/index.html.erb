<h1>Alarms#index</h1>

<%= link_to "Add a new alarm", new_alarm_url %>

<ul class="alarms">
  <% @alarms.each do |alarm|  %>
  <li>
  <%=alarm.title%>
  <%= button_to "Upvote", alarm_upvote_url(alarm) %>
  </li>

  <%end %>
</ul>
